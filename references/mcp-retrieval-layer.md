# MCP 实时检索层 · holiday-data-mcp（v1.0, v3.8 接入）

## 1. 定位与来源

`holiday-data-mcp` 是节假日消费数据领域的**结构化实时检索入口**（MCP 协议, 已配置于 `~/.workbuddy/mcp.json`, 地址 `https://ai-x-ai.online/mcp`, Bearer 鉴权）。它查询的数据后端即本技能历史基线**同源数据**（`D:\AISpace\01-projects\260824_holiday_data_mcp\data\data_set`, 上游 atomgit 仓库 `g_ww/holiday_data` 的衍生索引）。

> **不是替代**: atomgit `git clone/pull`（阶段一 4c, `data_set/holiday_data/`）继续作为**原始 CSV 落盘与跨期沉淀（回写上游）**的唯一源;MCP 是其上的**结构化检索互补层**——无需 clone 即可即时按条件取数,且 `compare_across_years` 自带口径/单位不一致告警。三者（MCP + atomgit 基线 + 联网检索）**互为交叉验证**。

## 2. 6 个工具与技能阶段映射

| 工具 | 用途 | 对应技能阶段 |
| --- | --- | --- |
| `get_schema` | 返回 22 字段字典 + 可信度 A/B/C/D/? + 数据性质（实际/预计/推算）+ 引用纪律 | 阶段二（口径字段依据,与 `caliber-dictionary.md` 同源一致） |
| `list_holidays` | 18 个「年份×节日」数据集总览（行数/现象数/预计数/主题清单） | 阶段一（确认本年/本期数据可用性） |
| `query_data_points` | 按 年份/节日/主题/关键词/最低可信度/数据性质 过滤 L1 结构化数据点,每行带 可信度/口径/URL/采集日期 | 阶段一/二（L1 取数初稿） |
| `query_phenomena` | 检索 L2 现象素材（标签/主题/描述/来源URL/采集日期） | 阶段一/二（L2 取数初稿） |
| `get_data_point_detail` | 按 `id` 取回完整 22 字段溯源（口径版本/基期/缺口标记/发布时间/完整 URL） | 阶段二/三（精确溯源,入真实性说明逐条溯源表） |
| `compare_across_years` | 跨年对比同一指标,自动检测口径类型/版本/单位不一致并告警,可选 per-day 折算 | 阶段二第 9 步 / 阶段四 纵向同比 |

## 3. 调用纪律（与本技能口径治理一致）

1. 引用必附 `URL` + `采集日期`（MCP 已内附,须原样进入数据集,反模式第 2 条）。
2. **数据性质严格分离**: `actual`=实测 / `forecast`=预计 / `estimate`=推算;预计/推算**不得当实测**（反模式第 3、11 条）。
3. `reliability=?` 表示等级未知,**不得当 A/B/C/D 引用**;优先引用 A/B 级。
4. `compare_across_years` 返回 `warnings`（口径不一致 / 单位不一致）时,**必须先对齐口径再比较**;未折算绝对量对比一律判为口径错误（反模式第 7、10 条）。其 per-day 折算值可直接用于纵向模块。
5. **跨年可比四元键铁律不变**: `(节假日, canon(数据点), 口径类型, 地域scope)`,四元一致且覆盖 ≥2 年才可做同比/环比;否则标「单年/不可比」,不得编造趋势线（反模式第 12 条）。

## 4. 工作流接入点

- **阶段一第 4d 步（取数交叉验证）**: 在 4c 拉取 `data_set/holiday_data/` 后,调用 `list_holidays` 确认本年/本期数据集可用性; `query_data_points` / `query_phenomena` 取 L1/L2 初稿,与 4c 基线及联网检索结果**交叉比对**,差异点须复核口径后再入库。MCP 返回数据映射进 22 字段口径治理时,逐条核对 `可信度等级`/`数据性质`/`口径类型`。
- **阶段二第 9 步（纵向归集）**: 调用 `compare_across_years` 取去年同期/本年上期代表值,**优先采用其自动 per-day 折算与口径/单位不一致告警**;结果与 `data_set/holiday_data/` 取数交叉验证,共同喂给四元键可比判定。
- **阶段三（溯源）**: `get_data_point_detail` 按 `query_data_points` 返回的 `id` 补回完整 22 字段,写入真实性说明「逐条溯源表」,确保每条数据可精确回溯口径版本与发布时间。

## 5. 离线降级

- MCP 不可达（网络/鉴权失败）时,**跳过 MCP 检索**,回退到 `data_set/holiday_data/` 与联网检索,不中断流程、不编造数据。
- 报告页脚/交付说明标注 `MCP 未获取（离线/不可达）`;纵向模块以 `data_set/holiday_data/` + 网页检索为准。
- 鉴权信息（Bearer Token）来自 `~/.workbuddy/mcp.json`,**不得写入技能交付物或公开产物**。

## 6. 与历史基线（atomgit）的关系小结

```
            ┌─────────────────────┐
            │  holiday-data-mcp   │  ← 结构化即时检索层（v3.8 接入）
            │  (无需 clone,带口径告警)│
            └──────────┬──────────┘
                       │ 互为交叉验证
            ┌──────────┴──────────┐
   ┌────────▼───────┐      ┌──────▼──────────────┐
   │  atomgit 基线   │      │   联网检索（≥8 轮）   │
   │ dataset/hdr/    │      │   9 大主题 + 探索性   │
   │ 原始 CSV 落盘    │      │   同源双录 L1+L2      │
   └────────┬───────┘      └──────┬──────────────┘
            └──────────┬──────────┘
                       ▼
              L1 数据集(22字段) + L2 现象素材库
              （三源交叉验证后定稿, 任一不可达均降级不中断）
```
