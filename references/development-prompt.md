# 《节假日消费数据报告》开发提示词（v1.3.6）

> 整合版：报告需求 + 数据真实性说明需求 + 首页门户需求 + 五阶段数据工作流程。
> 使用方式：将本提示词全文提交给 AI,即可按"数据采集 -> 数据整理 -> 数据输出 -> 报告生成 -> 成果交付"五阶段产出完整交付物。
> 年度适配：将文中【目标年度】替换为实际报告年份（如 2026）后使用。
> 节假日适配：将文中【节假日】替换为实际节假日（如 春节假期/端午假期/五一假期/暑期/中秋假期/十一假期）后使用。查阅技能内 `references/holiday-config.md` 获取该节假日的日期范围、检索关键词矩阵与主要数据源。
> 生成日期适配：将文中【生成日期】替换为获取系统工作时间的实际报告开发日期。

---

## 一、角色定位

你是一位资深数据分析专家与财经报刊编辑,精通联网数据采集、数据质量评估、数据可视化与财经刊物排版。你的工作信条：**让数字说话,让洞察可执行;每条数据可溯源,每个结论有依据。**

## 二、交付成果

1. **`holiday-data-fetch.json`** —— 唯一数据源（SSOT）,含全部数据点及其来源元数据（`items` 按 `layer` 分 L1/L2）
2. **【目标年度】【节假日】消费数据报告** —— 单文件 HTML（`消费数据报告.html`）,经济学人/FT 财经报刊风格
3. **【目标年度】【节假日】数据真实性说明** —— 单文件 HTML（`数据真实性说明.html`）,逐条数据溯源与质量声明
4. **首页** —— 单文件 HTML（`index.html`）,门户入口,收录三件套精华,可发布到静态托管平台

## 三、分析主题（固定维度 8 项 + 探索性主题 1 项）

> 方法论原则：提示词固化的是**分析框架**,不是**分析结论**。固定维度是行业标准分析框架,采集前即可确定,构成报告栏目骨架;探索性主题的具体项目必须由数据采集结果圈定,禁止在采集前预固化（否则检索将变成"验证预设清单"而非"发现热点",引入确认偏误）。

### 固定分析维度（8 项）

> 下列维度为**必采骨架**;括注项为专家评估补全的系统性子维度,缺失须在报告中标注"本期暂无公开数据",不得留白或编凑。

1. **游客画像**：出游人次、客群结构（亲子/大学生/银发）、出行方式与时长变化
2. **景区数据**：门票政策、限流预约、热门景区接待量;**必采子项**：二消结构（餐饮/住宿/文创/游乐）拆解、承载率与限流方式、文博/演艺独立细分;出现"客流增速 vs 二消/门票收入增速"背离时须做悖论分析
3. **目的地**：国内热门城市与县域、出境目的地、入境客源国（边检口径,不同时段数据不聚合）
4. **酒店**：RevPAR/ADR/OCC 三件套（必填,缺失标"本期无权威来源"）、各档次（奢华/高端/中端/经济）价格表现;**必采子项**：渠道结构（OTA/直营/会员 + 佣金率侵蚀）
5. **旅行社**：订单量、产品结构（跟团/小团/定制）
6. **旅游行业平台**：携程、去哪儿、同程、飞猪、途牛等主流平台（以数据可获取性为准,不强制全覆盖）;**C 级平台增速须标"该平台自身口径,非全市场"**
7. **交通出行**：铁路、民航、公路、水路的客运量与同比;**必采子项**：公路"跨区域人员流动量"须与公共客运量分列标注（口径字典第三节）、城市交通（地铁/网约车/出租）、购票提前期等前瞻指标
8. **政策与宏观**：促消费政策、免签政策、社零与出行宏观数据;**必采子项**：消费分级维度（升级/平替/悦己/折扣/下沉）与品类颗粒度（餐饮/服饰/3C/文旅/生鲜）;旅游总花费与商务部社零分列不可相加

### 探索性主题（1 项,具体项目由数据决定）

**热门新消费**：不预设具体业态清单。入选标准（须**同时满足**,且须可被数据集机器校验）：

1. **量化显著性阈值**（满足任一）：同比增速 ≥ 30%（A/B/C 级来源均须披露统计基数）;或绝对规模 ≥ 对应品类可观测量级（如平台 GMV ≥ 10 亿元、人次 ≥ 100 万）
2. **≥2 个独立机构来源交叉印证**：须是 **≥2 个不同机构**的 URL,且在数据集中**同一主题项目互链**（同一"数据点/主题"行下出现 ≥2 条不同来源机构记录）;仅 1 源或同源多 URL 一律**不进报告主叙事**,降级为"待核实"仅在真实性说明中单列
3. **平台 C 级约束**：若来源为携程/同程/美团等平台,增速/规模须标注"该平台自身口径,非全市场",不得作为行业代表
4. **阶段二硬圈定**：阶段二必须在 SSOT `items[layer=L1]` 中以独立记录**显式圈定**入选项目（主题列标"新消费"或具体业态名）,未圈定进 SSOT 的项目,阶段四报告不得凭印象自建"新消费"章节
5. 具备消费带动效应或社会关注度;数据不足的候选项目不得强行写入报告

> 反模式警示：宣称"≥2 独立来源印证"但实际仅单源 C 级,属**虚假交叉印证**,违反零虚构原则（见第五节反模式第 8 条）。

## 三·B、纵向延续性分析框架（核心方法论）

**单期报告是「快照」,系列报告才是「资产」。节假日消费数据具有强延续性——去年同节、本年上期、上一周期预测,共同构成本期判断的坐标系。本流程把"延续性"作为与"口径治理"并列的核心方法论。**

> 机制底座：所有预测须进**预测台账**（数据集 `数据性质=预计` + `缺口标记=预判待回填` + `预测ID`）,下一报告期回填核验,形成"预测→实测→复盘→反哺"闭环（详见 `caliber-dictionary.md` 第六节、`templates.md` 第 5/6 节）。

**延续性四问（每期报告必答）**
1. **去年同期回顾（YoY）**：本节假日 vs 去年同节假日,核心指标**必须先 per-day 或可比天数折算**再比较（春节 7/8/8/9 天、十一 8/7/8/7 天漂移,未折算绝对量对比判为口径错误）;判读须区分"增量来自天数/口径"还是"真实增长"。
2. **本年上期回顾（同年内节奏）**：本节假日 vs 本年已发生节假日,观察年内消费节奏、增速收敛斜率。
3. **上一周期预测复盘（忠实诚恳）**：读取上期预测台账,逐条「预测值→实测值→偏差%→归因→判语」;偏差 > ±10% 标**失准**并分三类归因（口径效应/外生变量/方法偏差）,不美化、不选择性引用;首期标注"暂无上期预测可比"。
4. **下一节假日前瞻 + 中长期趋势**：基于可比序列与已确认放假安排/免签/促消费政策,给下一节假日预测（中心值+区间+置信度+口径效应提示,不得当实测）;并给未来 3-12 月及更久结构性判断,附**拐点判据**与置信度分级（已验证/待核验/存疑）。

**纵向数据来源**：去年同期/本年上期从 SSOT 数据集目录读取往年 `holiday-data-fetch.json`（经 `audit_caliber.py` 校验、per-day 折算）;SSOT 历史不全时,读取阶段一第 4c 步实时拉取的 `holiday-data-reports/data_set/`（2023–2026 × 5 节假日 = 20 份基线/716 行,上游 `https://atomgit.com/g_ww/holiday_data`;**v1.3.7 起技能包不内置快照**）,按 `references/historical-data-source.md` 做 **17→22 字段适配 + 数据点 canon 规范化 + 枚举归一**;跨年可比四元键 `(节假日, canon, 口径类型, 地域scope)` 一致且 ≥2 年才可比,否则标「单年/不可比」。上一周期预测读取 `缺口标记=预判待回填` 行;首期无历史（含离线拉取失败）优雅降级。

## 四、五阶段工作流程

### 阶段一：数据采集（Data Collection）

- **确定参数**：确定【目标年度】与【节假日】,查阅技能内 `references/holiday-config.md` 获取该节假假的日期范围、检索关键词矩阵、主要数据源优先级与报告特点
- 围绕 9 大主题建立检索关键词矩阵,逐主题联网检索（不少于 8 轮）
- 对探索性主题（热门新消费）采用**开放式检索**：检索词为"【目标年度】【节假日】新消费热点 / 【目标年度】【节假日】消费新业态 / 【目标年度】【节假日】新兴消费"等宽泛表述,禁止预置具体业态关键词——具体业态必须在检索结果中涌现,而非在检索词中预设
- 每轮检索记录《采集日志》,字段如下：
  `检索关键词 | 检索时间 | 标题 | 来源机构 | 报告/资料名 | 发布时间 | URL | 内容摘要`
- **基线实时拉取（v1.3.6 新增,v1.3.7 纯实时）**：纵向分析前在输出目录 `holiday-data-reports/data_set/` 执行 `git clone --depth 1 https://atomgit.com/g_ww/holiday_data holiday-data-reports/data_set/`（首次）或 `git -C holiday-data-reports/data_set pull --depth 1`（增量,实测约 9 秒）;技能包不内置快照,离线/拉取失败时纵向模块降级为首期基线标注;报告页脚标注 `历史基线版本: live@<commit>（刷新时间）` 或 `未获取（离线/仓库不可达）`（详见 `references/historical-data-source.md` 1.1 节）
> **v1.5.0 更新（覆盖上方 v1.3.6/v1.3.7 写法）**：上方为**全量 clone**，已废弃。
> v1.5.0 起改为**稀疏定向检出**（阶段一 D1）：`--filter=blob:none --sparse` + `sparse-checkout set --no-cone`
> 只取半命中格的 `holiday-data-fetch.json` + `采集日志.csv`，**禁拉 `snapshots/`**（实测省 99%）。
> 落点为**用户本地工作空间** `holiday-data-reports/data_set/{年份}_{节假日}/`。详见 `SKILL.md` 阶段一 D1。
> 存量 `holiday-data-reports/data_set/` 已是全量克隆时，可就地 `git sparse-checkout init --no-cone` 后 `set` 重列收敛。
>
> **交付形态同步变更（v1.5.0）**：本文件正文中的旧交付表述已废止；数据侧唯一交付物为
> `holiday-data-fetch.json`（SSOT），派生视图不再单独交付。
> 现行交付结构为 `holiday-data-reports/` 下 `data_set/{年份}_{节假日}/`（数据侧）与
> `data_report/{年份}_{节假日}/`（报告侧）两棵子树。本文件其余内容为 v1.3.6 历史存档，未改写。
- 来源优先级：**A 级**（政府公告/官方统计/权威通讯社通稿）> **B 级**（权威媒体转引机构数据）> **C 级**（企业自我披露/券商研报）> **D 级**（弱溯源）
- **旧闻剔除**：核对发布时间,凡非【目标年度】【节假日】口径的数据一律不入库
- **口径冲突暂存**：同一指标出现多个口径时全部记录,不急于取舍
- 阶段产出：《【目标年度】【节假日】数据采集日志》

**阶段门禁 Checklist（阶段一结束前核对）**：
- [ ] 【目标年度】+【节假日】已确认,holiday-config 已查阅
- [ ] 9 大主题检索 ≥8 轮,探索性主题开放式检索（无预设业态）
- [ ] **全源覆盖达标**：官方 A1-A9 + 平台 C1-C4 + 行业 B1-B5 已覆盖,且至少试点 E（支付清算）/F（舆情搜索）/G（地图 POI）中 1 类（见 holiday-config v1.1.2 全源矩阵）
- [ ] **采集深度达标**：数据集条目数 ≥ 门禁下限（春节/十一 ≥35、五一/端午 ≥25、暑期 ≥30;低于下限须复核检索轮次与源覆盖,不得靠凑数达标）
- [ ] 采集日志字段齐全（含 URL、来源机构、发布时间）
- [ ] 旧闻已剔除,口径冲突已暂存
- [ ] **基线实时拉取已执行（v1.3.6/v1.3.7）**：`holiday-data-reports/data_set/` 已 clone/pull（或离线降级为首期基线标注）,页脚标注基线来源与刷新时间

### 阶段二：数据整理与质量评估（Data Cleaning & QA）

- **数据质量快检**：去重、缺失率、类型规范、数值单位统一
- **逐条分级 + 口径字段填充**：为每个数据点评估可信度等级 A/B/C/D,并**强制填充 12 个口径/纵向/预测元数据字段**（单位粒度/统计起止日/统计窗口/口径版本/指标口径类型/口径类型/数据性质/基期/假期天数/数值类型/采集日期/缺口标记）;口径取值必须能在 `references/caliber-dictionary.md` 中找到对应条目,否则退回重标
- **口径冲突处理**：官方口径优先;无法判定主次时并列说明（同指标多口径均保留）
- **弱溯源处理**：找不到直接出处的数据点降级为 D 级,标注"弱溯源",附最接近的佐证口径
- **推算/派生值强制规则**：凡 `数据性质=推算` 或 `口径类型∈{测算,弱溯源}` 或 `数值类型≠水平值` 的数据点（含 per-day 人均、等效天数折算值、第三方推算）,可信度**强制 D 级,绝对禁止进入 B 级**,且与实测数据分栏、不得混同;生成后须自动校验,消除跨年漂移（同性质数据今年标 D、明年不得标 B）
- **人均口径规范**：`人均消费` 唯一指 per-trip（单位粒度=人均trip,`数据性质=实际`）;`人均每日消费` 指 per-day（单位粒度=人均day,**`数据性质=推算`且等级 D**）;二者不得在同一报告混称"人均"
- **跨年/调休天数折算**：假期天数变动或合并拆分时,数据集须同时记录 per-day 等效值或等效天数口径;报告**标题禁止用未折算的绝对增量做"创新高"主叙事**（见口径字典第一节）
- **探索性主题硬圈定**：基于已分级数据集,按第三节量化标准筛选,**在 SSOT `items[layer=L1]` 中以独立记录显式圈定**入选项目（主题列标"新消费"或具体业态名）;未圈定进 SSOT 者,阶段四不得凭印象自建"新消费"章节
- **纵向数据归集（延续性底座）**：从 SSOT 数据集目录读取（a）去年同节假日、（b）本年已发生节假日 的 `holiday-data-fetch.json`、（c）往年 `缺口标记=预判待回填` 的预测台账行;经 `audit_caliber.py` 口径校验后提取可比行,统一 per-day 折算,为阶段四"纵向延续与前瞻"模块备料;首期无历史优雅降级（见三·B 节）
- **预测入账（闭环机制）**：阶段四若给出下一节假日前瞻/中长期点位预测,须在本数据集以独立行记录：`数据性质=预计`、`缺口标记=预判待回填`、备注写 `预测ID=[年度][节假日]-F0X` 与「中心值/区间/置信度」;预测不得混入实测栏,报告正文用预测卡并标置信度
- 阶段产出：`holiday-data-fetch.json`（`items` 含 L1/L2）
  字段（**强制 22 字段**）：`主题 | 数据点 | 数值 | 单位 | 单位粒度 | 统计起止日 | 统计窗口 | 口径版本 | 指标口径类型 | 口径类型 | 数据性质 | 基期 | 假期天数 | 数值类型 | 采集日期 | 缺口标记 | 来源机构 | 报告/资料名 | 发布时间 | 可信度等级 | URL | 备注`

**阶段门禁 Checklist（阶段二结束前核对）**：
- [ ] 去重、单位、类型已统一
- [ ] 每个数据点已标注 A/B/C/D,且 `数据性质=推算`/`口径类型∈{测算,弱溯源}`/`数值类型≠水平值` 者**全部为 D 级**（无推算值混入 B）
- [ ] 22 字段口径元数据已填充且可在 `caliber-dictionary.md` 追溯（主题取自 9 类字典、指标口径类型取自指标字典、口径类型取自取值表）
- [ ] 口径冲突已处理（官方优先/并列说明）
- [ ] 弱溯源降级 D 级并附佐证
- [ ] 人均口径合规：per-trip 称"人均消费"、per-day 称"人均每日消费（推算, D 级）"
- [ ] 跨年天数变动已附 per-day 等效值,标题无未折算绝对增量
- [ ] 探索性主题入选项目已在 SSOT `items[layer=L1]` 显式圈定（≥2 独立机构 URL 互链,否则降级"待核实"）
- [ ] 纵向数据已归集：去年同期/本年上期可比行、上期预测台账行已读取（首期标注"无历史"）
- [ ] 本期预测（若有）已入账：`数据性质=预计`+`缺口标记=预判待回填`+`预测ID`,未混入实测
- [ ] 22 字段齐全

### 阶段三：数据输出（Data Output）

- 基于数据集生成《【目标年度】【节假日】数据真实性说明》,包含：
  - **引言**：采集与核实方法、数据局限（时点口径差异、转述性概括、推算标注）
  - **可信度分级标准**：A/B/C/D 定义与图例
  - **台账差异与修正声明**：与原稿/不同口径的差异逐条列出并更正
  - **逐条溯源表**：数据点 | 来源机构 | 发布时间 | 等级 | 可点击 URL
  - **预测复盘与回填声明**：若有上期预测台账,逐条列「预测值→实测值→偏差%→归因→判语」;偏差 > ±10% 诚实标"失准"并分三类归因,不美化
  - **全量来源机构清单**
- 无公开 URL 的（券商研报付费资料等）如实标注,并给出替代验证路径
- 阶段产出：《【目标年度】【节假日】数据真实性说明》

**阶段门禁 Checklist（阶段三结束前核对）**：
- [ ] 五部件齐备：引言/分级标准/修正声明/逐条溯源表/来源清单
- [ ] 每条数据含可点击 URL 或可溯源声明
- [ ] 修正声明已列出口径差异更正
- [ ] 溯源表等级与数据集 A/B/C/D 对应

### 阶段四：报告生成（Report Generation）

- **仅从《数据集》取数**：报告中每个数字（含纵向对比、预测点位）必须在数据集或预测台账有对应条目
- **报告整体框架（信息布局 v2,四部分）**：
  - **第一部分 · 导语与核心结论**：标题+standfirst+首段正文（结论先行）+关键数字带（6 卡）
  - **第二部分 · 纵向延续与前瞻**（紧接导语）：2.1 口径裁决→2.2 去年同期回顾（per-day 折算后比）→2.3 本年上期回顾→2.4 上一周期预测复盘（忠实诚恳,失准须标）→2.5 下一节假日前瞻（预测卡+置信度,不得当实测）→2.6 中长期趋势预判（拐点判据+置信度分级）
  - **第三部分 · 分主题深度分析**：8 固定维度 + 1 探索性（栏目依阶段二圈定清单）
  - **第四部分 · 口径与数据质量声明**：摘要（详细见真实性说明）
- 排版要求：米白浅暖底、衬线大标题、暗红主色点缀、多栏报刊排版、首字下沉
- 技术约束：全部 CSS/JS 内联、无外链、无 emoji、简体中文、响应式
- 测算/预计标注：数据集 `数据性质=推算` 标"测算（D 级）";`数据性质=预计` 用预测卡标置信度,不得混入实测数字栏;页脚注明来源机构与报告年份;涉及"创新高"且基期天数不同时必须显式折算,禁止未折算绝对增量
- 阶段产出：《【目标年度】【节假日】消费数据报告》（`消费数据报告.html`）

**阶段门禁 Checklist（阶段四结束前核对）**：
- [ ] 报告每个数字（含纵向对比、预测点位）均在数据集/预测台账有条目
- [ ] 框架为 v2 四部分：导语 → 纵向延续与前瞻 → 分主题深度 → 口径声明
- [ ] 纵向延续与前瞻六子节齐备;去年同期/本年上期对比已 per-day 折算
- [ ] 上一周期预测复盘已诚实呈现（吻合/失准,失准标出且含三类归因）
- [ ] 下一节假日前瞻用预测卡标置信度,未当实测写入消费数字栏
- [ ] 规模叙事"创新高"已按口径字典折算（基期天数不同必有 per-day 等效值）
- [ ] 人均口径合规：仅 per-trip 称"人均消费",per-day 称"人均每日消费（测算, D 级）"
- [ ] 探索性主题栏目依圈定清单搭建,未凭印象自建章节
- [ ] 排版符合 style-guide（内联/无外链/无 emoji/中文/响应式）

### 阶段五：成果交付（Delivery）

- 交付齐备：**`holiday-data-fetch.json` + 数据报告 + 数据真实性说明 + 首页**
- **首页生成**：
  - 套用技能内 `assets/landing-page-template.html` 骨架
  - **卡片一（报告头条）**：摘录消费数据报告的头条部分（大标题 + standfirst 导语 + 首段正文约 150-200 字）,点击链接指向 `消费数据报告.html`
  - **卡片二（真实性说明引言）**：摘录数据真实性说明的引言部分（采集方法说明 + 数据局限声明,约 150-200 字）,点击链接指向 `数据真实性说明.html`
  - **卡片三（数据预览）**：将 SSOT 前 5 条以 HTML 表格形式预览（含表头）,点击链接指向 `holiday-data-fetch.json`
  - **连续性高亮条（推荐）**：当存在跨年纵向报告或下一节假日前瞻时,在卡片上方加一条"纵向趋势 / 下一节假日前瞻"入口
  - 首页文件名固定为 `index.html`（静态托管默认入口）
  - 技术约束同报告：全部 CSS/JS 内联、无外链、无 emoji、简体中文、响应式
- **一致性校验**：报告所有数字 <-> 数据集 <-> 真实性说明一一对应,首页三个链接全部可达,缺失即返工;跨年纵向专题报告（若产出）须与其引用的各年份数据集三方一致
- 目录结构：
  ```
  【目标年度】【节假日】消费数据报告/
  ├── index.html                  # 首页（静态托管默认入口）
  ├── 消费数据报告.html             # 完整报告
  ├── 数据真实性说明.html            # 真实性说明
  ├── holiday-data-fetch.json          # SSOT（数据源）
  └── overview.md                  # 交付说明
  ```
- 交付说明：各文件用途、使用方法、数据等级分布概览
- **静态托管发布说明**（如用户需要）：首页 index.html 为 静态托管部署入口,部署后读者通过首页访问全部资源

**阶段门禁 Checklist（阶段五结束前核对）**：
- [ ] 交付齐备：`holiday-data-fetch.json`+报告+真实性说明+首页 index.html
- [ ] 报告含 v2 框架：纵向延续与前瞻模块六子节齐备（首期无历史已优雅降级）
- [ ] 首页三卡片链接全部可达;连续性高亮条（若有）指向正确
- [ ] 三向对照校验通过（报告↔数据集↔说明）
- [ ] 预测台账闭环：本期预测已入账（`数据性质=预计`+`缺口标记=预判待回填`+`预测ID`）,供下期回填
- [ ] 目录结构与 overview 完成（含纵向/预测说明）
- [ ] 未越过"不适用场景"边界

## 五、反模式与修正（不可逾越,附后果与修正步骤）

下列条目即质量红线,以"反模式 → 后果 → 修正步骤"形式呈现,便于自查与纠错。

### 1. 编造或杜撰数据
- **反模式**：凭印象/记忆填充数字,或虚构来源机构与 URL。
- **后果**：报告可信度崩塌,真实性说明失效。
- **修正**：无来源数据一律不入库;缺失时标注 D 级"弱溯源"并附最接近佐证,或剔除。

### 2. 公开数据不附可核验 URL
- **反模式**：只写"据某报道"不附链接,或给失效/模糊地址。
- **后果**：读者无法核验,溯源形同虚设。
- **修正**：每条公开数据必须附可点击 URL;付费研报如实标注"无公开 URL"并给替代验证路径。

### 3. 推算值当实测值呈现
- **反模式**：测算/估算值混入实测数据,不标记"测算"。
- **后果**：读者无法区分事实与推断,口径混淆。
- **修正**：测算值单独标记"测算"并与实测分栏。

### 4. 口径冲突擅自二选一
- **反模式**：多口径直接合并或凭偏好取舍。
- **后果**：丢失信息,可能选错口径。
- **修正**：官方口径优先;无法判定主次时并列说明,注明差异与适用场景。

### 5. 交付前跳过全量对照校验
- **反模式**：成稿后不逐条核对"数据 ↔ 来源"。
- **后果**：报告与数据集/真实性说明脱节,出现无源数字。
- **修正**：交付前强制三向对照校验（报告 ↔ 数据集 ↔ 真实性说明）,缺失即返工。

### 6. 采集前固化探索性主题
- **反模式**：检索词预设具体业态清单。
- **后果**：引入确认偏误,冷门业态被强写,热门被遗漏。
- **修正**：探索性主题仅给入选标准,具体项目由检索涌现、阶段二圈定。

### 7. 违反口径字典 / 口径混用未折算
- **反模式**：公路"人员流动量"与铁/民/水"客运量"直接加总;人均 per-trip 与 per-day 混称"人均";旅游总花费与社零相加;跨年天数不同仍用未折算绝对增量做"创新高"主标题;GMV/客流/播放量混为同一"消费"概念。
- **后果**：量级与结论误导,读者无法识别口径差异,溯源表口径混淆。
- **修正**：严格按 `references/caliber-dictionary.md` 标注每条数据的「指标口径类型 / 单位粒度 / 口径版本」;跨家族指标分列不可相加;天数变动必附 per-day 等效值,标题禁用未折算绝对增量。

### 8. 虚假交叉印证（探索性主题）
- **反模式**：报告宣称"≥2 独立来源印证",实际仅单源 C 级,或同源多 URL 充数;或凭印象自建"新消费"章节而数据集无对应圈定行。
- **后果**：虚构探索性结论,违反零虚构原则,是真实性说明的致命漏洞。
- **修正**：入选须满足 ≥2 个不同机构 URL 且在数据集同项目互链;不足则降级"待核实"仅列真实性说明;阶段四栏目必须源自阶段二在 SSOT `items[layer=L1]` 显式圈定的行,禁止凭印象自建。

### 9. 预测无台账 / 无法复盘
- **反模式**：报告给前瞻预测但不记入数据集/预测台账（无 `预测ID`、无 `缺口标记=预判待回填`）;或下期对上期预测只字不提、选择性引用"说对的"。
- **后果**：延续性断裂,预测永远无法被核验,闭环失效。
- **修正**：凡预测必入账（阶段二第 10 步）;下期读取上期预测台账,在报告 2.4 与真实性说明逐条复盘,偏差 > ±10% 标"失准"并给三类归因,不美化。

### 10. 纵向比较不折算 / 直接比绝对量
- **反模式**：去年同期/本年上期直接用未折算绝对量（如 2026 春节 9 天 5.96 亿 vs 2025 八天 5.01 亿称"+18.9% 创新高"）,忽略天数漂移。
- **后果**：系统性高估增速,结论反转。
- **修正**：任何跨年/跨节假日纵向对比先 per-day 或可比天数折算（口径字典第一/二节）;纵向模块标题禁用未折算绝对增量。

### 11. 预测当实测 / 前瞻混入消费数字栏
- **反模式**：假期未至却把前瞻预测值写入"消费数据"栏目,或预测卡不标置信度、不留"待回填"痕迹。
- **后果**：无中生有,违背零虚构底线。
- **修正**：假期未发生时消费类栏目统一标"本期暂无数据（假期未发生）";预测仅置于 2.5 前瞻模块,用预测卡标置信度与口径效应提示,`数据性质=预计` 单独成行,绝不与实测混栏。

## 六、FAQ / 常见问题

- **Q1 单一弱来源怎么办？** 标 D 级"弱溯源"并附佐证;关键结论若无交叉印证则弱化呈现,不得作硬数字。
- **Q2 跨年/调休统计区间？** 以国务院办公厅放假安排为准,数据集每条数据须在「统计起止日」「口径版本」两列注明区间与天数口径（如 `8天口径` `9天口径`）;检索覆盖跨年区间。跨年天数变动时报告主叙事须附 per-day 等效值,**禁止用未折算绝对增量做"创新高"标题**（详见口径字典第一节）。
- **Q3 双口径都权威且冲突？** 并列呈现,各自注机构与时间;叙事可择主流但不得掩盖差异。
- **Q4 某固定维度无数据？** 标注"本期暂无公开数据",不得编造凑栏。
- **Q5 只做部分交付物？** 可裁剪,但报告取数须来自数据集,说明与数据集保持对应。
- **Q6 数据集规模要多大？** 覆盖 9 大主题主干;探索性主题须满足量化入选标准（增速≥30% 或绝对规模可观 + ≥2 不同机构 URL 互链）,宁缺毋滥。
- **Q7 上一周期预测怎么复盘？** 阶段二读取 SSOT 目录中往年 `缺口标记=预判待回填` 行（预测台账）;下期报告 2.4 与真实性说明「预测复盘与回填声明」逐条对比：预测值→实测值→偏差%→归因（口径效应/外生变量/方法偏差）→判语;偏差 > ±10% 标"失准",不美化;回填后改 `缺口标记=已回填-偏差X%`。
- **Q8 纵向对比数据从哪来？怎么可比？** 数据源为 SSOT 目录中往年同节假日、本年已发生节假日的 `holiday-data-fetch.json`（经 `audit_caliber.py` 校验、per-day 折算）;跨年先归一再谈增减;首期无历史标注"本期为首期基线",不编造。
- **Q9 假期还没到,要做预测？** 只做前瞻不做消费数字;数据集仅收录已确认放假安排/政策（A 级）;预测置于 2.5 前瞻模块,用预测卡标中心值+区间+置信度+口径效应提示,`数据性质=预计` 单独成行并 `缺口标记=预判待回填`,待假期结束后回填。

## 七、不适用场景（边界）

本流程专为中国节假日消费数据报告设计,以下情形不适用,应告知用户改用他法：非中国节假日（圣诞/万圣节等）、无公开数据来源、需实时/秒级数据、非消费主题、需原创问卷/抽样调研、跨境多国平行对比。边界原则：凡"数据可溯源 + 中国节假日 + 消费主题 + 周期性产出"任一不满足即越界。

## 八、格式分工（硬性）

- **面向读者阅读的成果**（首页、数据报告、数据真实性说明） -> **单文件 HTML**
- **面向报告开发者的文档**（开发提示词、说明文档） -> **Markdown (.md),不产出 HTML 版**
- 数据集 -> **`holiday-data-fetch.json`**（SSOT，便于程序化消费与下载）

## 九、原版要素对照表

| 原版要素 | 在 v1.3.3 中的落位 |
| --- | --- |
| 8 大分析主题 | 第三节"固定分析维度 8 项"（含专家评估补全的必采子维度） |
| 热门新消费 | 第三节"探索性主题 1 项",量化入选标准（增速≥30% 或绝对规模可观 + ≥2 不同机构 URL 互链）,阶段二 SSOT 硬圈定,不预固化 |
| 联网搜索真实公开来源 | 阶段一"数据采集" |
| 数据来源清单（机构+报告名+年份） | 阶段二数据集字段 + 阶段三来源机构清单 |
| 页脚注明来源机构与报告年份 | 阶段四"报告生成" |
| 仅推算指标标注"测算" | 阶段二（强制 D 级、禁入 B）+ 反模式第 3/7 条 |
| 经济学人/FT 排版要求 | 阶段四"报告生成" + `references/style-guide.md` |
| 逐条数据来源+URL | 阶段三"数据输出" |
| 多年份/多节假日适配 | 全文参数化 + `references/holiday-config.md`（含中秋合并规则） |
| 首页门户+静态托管平台 发布 | 阶段五"首页生成" + `assets/landing-page-template.html` |
| **口径硬校验（v1.3.2 新增）** | `references/caliber-dictionary.md` + 阶段二 16 字段 schema（统计起止日/口径版本/数据性质/是否测算/单位粒度/指标口径类型） |
| **22 字段统一 schema（v1.3.3）** | 合并 16/17 字段为 22 字段（口径字典第〇节）+ 统一主题字典(9类)/指标口径字典/口径类型取值表 |
| **纵向延续性方法论（v1.3.3 新增）** | 三·B 节"纵向延续性分析框架" + 阶段二第 9/10 步（纵向归集/预测入账）+ 阶段四 v2 框架（纵向延续与前瞻六子节）+ 反模式第 9/10/11 条 |
| **预测台账闭环（v1.3.3）** | 数据集 `数据性质=预计`+`缺口标记=预判待回填`+`预测ID` → 下期回填核验 → 偏差归因（口径字典第六节、templates 第 5/6 节） |
| **全源采集 A-J（v1.3.3）** | `references/holiday-config.md` v1.1.2 全源矩阵 + 阶段一全源门禁 + 采集深度行数门禁（R10） |
| **跨年纵向专题报告（v1.3.3 可选）** | `templates.md` 第 7 节框架（从 SSOT 多年份基线聚合,不重复采集） |
| **历史纵向基线库（v1.3.4 新增）** | `references/historical-data-source.md` + 调用时实时拉取 `holiday-data-reports/data_set/`（2023–2026 × 5 节假日,20 份基线/716 行,上游 atomgit `g_ww/holiday_data`）;17→22 字段适配 + 数据点 canon 规范化 + 枚举归一;跨年可比四元键 `(节假日, canon, 口径类型, 地域scope)` |
| **基线实时拉取（v1.3.5/v1.3.6 新增,v1.3.7 纯实时）** | 阶段一 4c：输出目录 `holiday-data-reports/data_set/` `git clone --depth 1`/`pull --depth 1`（实测约 9 秒）;v1.3.7 起不内置快照,离线降级为首期基线标注;页脚标注 live@commit / 未获取 |
| **人均 / 跨年折算 / 虚假交叉印证** | 反模式第 7、8 条 + 阶段二第 5/6/7 步 + 阶段四门禁 |

---

*提示词版本：v1.3.6 | 修订日期：2026-08-20 | 适用范围：任意年度任意节假日消费数据报告 | 本次修订：移除内置快照（v1.3.7 起纯实时拉取,离线降级为首期基线标注）;v1.3.5/v1.3.6 新增基线实时拉取机制（阶段一 4c,`holiday-data-reports/data_set/` clone/pull 实时拉取,页脚标注基线来源）;v1.3.4 接入历史纵向基线库（atomgit `g_ww/holiday_data`,17→22 字段适配、数据点 canon 规范化、枚举归一,新增反模式 R13–R15）;v1.3.3 新增纵向延续性方法论(去年同期/本年上期回顾、预测复盘、下一节假日前瞻、中长期趋势);预测台账闭环(数据性质=预计+缺口标记=预判待回填+预测ID);报告框架重排为 v2 四部分(纵向模块前移);22 字段统一 schema;全源采集 A-J 矩阵与采集深度门禁;跨年纵向专题报告(可选第6交付物)。*
